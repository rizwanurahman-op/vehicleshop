// Type declaration for archiver-zip-encrypted (no official @types package)
declare module "archiver-zip-encrypted" {
    import { Transform } from "stream";
    const plugin: unknown;
    export = plugin;
}
